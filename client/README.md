# ATS-Mini CLI Terminal

Interactive command-line terminal for controlling ATS-Mini radio device.

## Installation

Requires the SDK:
```bash
pip install -e ../sdk
pip install -e .
```

## Usage

```bash
atsmini-terminal
```

The terminal comes with built-in help accessible via the `help` command.
